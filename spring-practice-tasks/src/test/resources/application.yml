spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.tool.schema: DEBUG
    org.hibernate.SQL: DEBUG


kafka:
  topic:
    notes:
      name: note-events-test
    notes-dlt:
      name: note-events-test.DLT